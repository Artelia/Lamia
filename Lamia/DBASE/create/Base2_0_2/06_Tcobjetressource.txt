# Nom_champ             TypePostGis              TypeSpatialite                          Option
#DJAN  SELECT * FROM Tcobjetressource   WHERE lpk_revision_end IS NULL
#QGIS SELECT * FROM Tcobjetressource
###pk_tcobjetressource;                  SERIAL PRIMARY KEY;     INTEGER PRIMARY KEY AUTOINCREMENT
###lpk_revision_begin;        INT ;                   INTEGER;                              Revision(pk_revision)
###lpk_revision_end;          INT ;                   INTEGER;                              Revision(pk_revision)

###lid_ressource;       INT;            INTEGER
###lid_objet;       INT;            INTEGER