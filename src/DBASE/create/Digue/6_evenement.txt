#DJAN SELECT  Evenement.* , Objet.datecreation, Objet.datedestruction, Objet.commentaire, Objet.libelle FROM Evenement INNER JOIN OBJET ON Evenement.id_objet = Objet.id_objet
#QGIS SELECT  Evenement.* , Objet.datecreation, Objet.datedestruction FROM Evenement INNER JOIN OBJET ON Evenement.id_objet = Evenement.id_objet
# Nom_champ             TypePostGis              TypeSpatialite                          Option
###id_Evenement                 INT;                     INTEGER;    INTEGER PRIMARY KEY AUTOINCREMENT
###id_objet                 INT;                     INTEGER;    NOT NULL
###gravite                 INT;                     INTEGER;    NOT NULL
###name ;                VARCHAR(200);            TEXT;   NOT NULL
###ref ;                VARCHAR(200);            TEXT;   NOT NULL
###store ;                VARCHAR(255);            TEXT;   NOT NULL
###listeparametres ;                VARCHAR(255);            TEXT
###parametres ;                VARCHAR(255);            TEXT
###ref ;                VARCHAR(255);            TEXT
###description ;                VARCHAR(255);            TEXT
